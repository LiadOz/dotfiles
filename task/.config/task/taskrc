# Taskwarrior 3.0 configuration

# XDG Base Directory paths
data.location=~/.local/share/task
hooks.location=~/.config/task/hooks

# Color theme (uncomment one to use)
#include light-16.theme
#include light-256.theme
#include dark-16.theme
#include dark-256.theme
#include dark-red-256.theme
#include dark-green-256.theme
#include dark-blue-256.theme
#include dark-violets-256.theme
#include dark-yellow-green.theme
#include dark-gray-256.theme
#include dark-gray-blue-256.theme
#include solarized-dark-256.theme
#include solarized-light-256.theme
#include no-color.theme

# Custom reports
report.in.columns           = id,description
report.in.description       = Inbox
report.in.filter            = status:pending limit:page (+in)
report.in.labels            = ID,Description

# Machine-specific settings and anything secret -- sync credentials, the
# encryption secret, certificate paths. Kept out of this repo: taskrc is
# tracked and the repo is public, so a routine `git add -A` would publish
# them. install.sh creates the file if it is missing; taskwarrior errors on
# an include it cannot read.
include ~/.config/task/taskrc.local
